function buildCorpus(scenes,target){const pools={idle:[],short:[],morning:[],sleep:[],feed:[],bath:[],play:[],affection:[],rareLove:[],tease:[],surprise:[],ack:[],return:[],comfort:[],memory:[]},seen=new Set(),sceneMap=new Map(),tokens=new Map();for(const sc of scenes||[]){sceneMap.set(String(sc.id),sc);const lines=(sc.lines||[]).filter(x=>x&&usable(x.text));for(let i=0;i<lines.length;i++){const l=lines[i];if(l.who!==target)continue;const t=cleanText(l.text),prev=i?cleanText(lines[i-1]?.text):'',e={text:t,date:sc.date||'',sceneId:String(sc.id||''),prev};if(t.length<=42)addPool(pools,'idle',e,seen);if(t.length<=16)addPool(pools,'short',e,seen);for(const[k,rx]of Object.entries(RX)){if(k==='rareLove')continue;if(rx.test(t)||rx.test(prev))addPool(pools,k,e,seen)}if(RX.rareLove.test(t)&&t.length<=58)addPool(pools,'rareLove',e,seen);if(t.length<=58)addPool(pools,'memory',e,seen);for(const w of ['おけけ','しょもしょも','オフトゥン','だん','かわいい','好き','すき','www','w','あーね','ほう','ふむ','うい','われわれ','カラダ・アラウ','はすはす','メロ'])if(t.includes(w))tokens.set(w,(tokens.get(w)||0)+1)}}for(const k of Object.keys(pools)){const lim=k==='idle'?1800:k==='memory'?1200:600;if(pools[k].length>lim){const step=pools[k].length/lim;pools[k]=Array.from({length:lim},(_,i)=>pools[k][Math.floor(i*step)])}}return{pools,sceneMap,tokens:[...tokens.entries()].sort((a,b)=>b[1]-a[1]).slice(0,8).map(x=>x[0]),count:new Set(Object.values(pools).flat().map(x=>x.text)).size}}
async function loadCorpus(){try{const scenes=await window.WareraData?.memories?.('../');if(scenes?.length)return buildCorpus(scenes,G)}catch(e){console.warn('grow corpus fallback',e)}return buildCorpus([],G)}
function fallbackEntries(key){return(FALLBACK[G][key]||FALLBACK[G].idle||[]).map(text=>({text,date:'',sceneId:'',prev:''}))}
function recentTexts(){return new Set((S.recent||[]).slice(-24).map(x=>x.text).filter(Boolean))}
function weightedPick(arr,key){arr=(arr&&arr.length?arr:fallbackEntries(key));const recent=recentTexts(),fav=S.favoritePhrases||{};let pool=arr.filter(x=>!recent.has(x.text));if(!pool.length)pool=arr;const weighted=[];for(const e of pool){let n=1+Math.min(4,Number(fav[e.text]||0));if(e.text.length<=18)n++;for(let i=0;i<n;i++)weighted.push(e)}return pick(weighted.length?weighted:pool)}
function entryFor(key){return weightedPick(C?.pools?.[key],key)}
function minsSince(action){const t=Number(S.lastActionAt?.[action]||0);return t?Math.max(0,(now()-t)/60000):9999}
function updatePoop(){if(S.poopDueAt&&now()>=S.poopDueAt&&!isAsleep()){S.poop=true;S.poopDueAt=0}}
function moment(){if(isAsleep())return{key:'sleep',emoji:'🌙',label:'すやすや寝てる',action:'sleep'};updatePoop();if(S.poop)return{key:'toilet',emoji:'💩',label:'なんか出てる',action:'toilet'};const vals=[['feed',minsSince('feed')/210,'🍚','なんか食べたそう'],['play',minsSince('play')/165,'🎮','ひまそう'],['bath',minsSince('bath')/540,'🫧','そろそろカラダ・アラウ'],['pat',minsSince('pat')/(S.traits.clingy>S.traits.independent?190:260),'💗','ちょっと構われたそう']].sort((a,b)=>b[1]-a[1]);if(vals[0][1]>=1)return{key:vals[0][0],emoji:vals[0][2],label:vals[0][3],action:vals[0][0]};const h=new Date().getHours();if(h>=21)return{key:'sleepSoon',emoji:'🌙',label:'夜になってきた',action:'sleep'};const quiet=S.traits.independent>S.traits.clingy*.95;return{key:'fine',emoji:quiet?'☀️':'✨',label:quiet?'ひとりでしょもしょもしてる':'なんでもない。たぶん。',action:''}}
function basePet(){const st=stage()[0];if(st==='egg')return'egg_idle.png';if(st==='baby')return'baby_idle.png';if(st==='child')return S.childType==='affection'?'child_happy.png':'child_plain.png';if(S.personality==='aloof')return'adult_cool.png';if(S.personality==='tsundere')return'adult_tsundere.png';return'adult_happy.png'}
function currentPet(){const st=stage()[0];if(st==='egg')return'egg_idle.png';if(isAsleep())return'blanket_rest.png';const m=moment();if(m.key==='play'&&minsSince('play')>300)return'sit_sad.png';return basePet()}
function reactionPet(a,repeat,rare){if(stage()[0]==='egg')return'';if(a==='feed')return Math.random()<.1?'eat_fish.png':'eat_onigiri.png';if(a==='pat')return rare&&stage()[0]==='adult'?'adult_love.png':'hold_heart.png';if(a==='sleep')return'blanket_rest.png';if(a==='play')return Math.random()<.5?'stand_left.png':'quarter_left.png';if(repeat>=4)return'crawl_angry.png';return''}
function setPet(file,ms=0,anim=''){const img=$('g12pet');if(!img)return;const egg=stage()[0]==='egg';const shown=egg?'egg_idle.png':file;img.src=ASSET+shown;if(anim){img.classList.remove('bounce','wiggle','squish');void img.offsetWidth;img.classList.add(anim);setTimeout(()=>img.classList.remove(anim),650)}if(ms)setTimeout(()=>{const el=$('g12pet');if(el)el.src=ASSET+(stage()[0]==='egg'?'egg_idle.png':currentPet())},ms)}
function recordRecent(type,text){S.recent.push({at:now(),type,text});if(S.recent.length>80)S.recent=S.recent.slice(-80)}
function repeats(a){const last=(S.recent||[]).slice(-8).filter(x=>x.type==='action');let n=0;for(let i=last.length-1;i>=0;i--){if(last[i].action===a)n++;else break}return n}
function contextualSpeech(a,matched,repeat){const st=stage()[0],rareChance=(st==='adult'?.055:st==='child'?.035:.018)+(S.favoritePhrases?Math.min(.02,Object.keys(S.favoritePhrases).length*.002):0);if(['pat','play'].includes(a)&&C?.pools?.rareLove?.length&&Math.random()<rareChance)return{entry:entryFor('rareLove'),rare:true};if(repeat>=3&&C?.pools?.tease?.length)return{entry:entryFor('tease'),rare:false};const map={feed:'feed',play:'play',pat:'affection',bath:'bath',toilet:'ack',sleep:'sleep',wake:'morning',return:'return'};let key=map[a]||'idle';if(!matched&&['feed','bath','play','pat'].includes(a)&&Math.random()<.45)key=Math.random()<.5?'ack':'tease';return{entry:entryFor(key),rare:false}}
function maybeMemory(force=false){if(!C?.pools?.memory?.length)return null;if(!force&&Math.random()>.045)return null;const e=weightedPick(C.pools.memory,'memory'),sc=C.sceneMap.get(String(e.sceneId));if(!sc)return null;currentMemory=sc;return{entry:e,scene:sc}}
function experienceGain(a,matched,repeat){let g=1;if(matched)g+=1;if(S.recent.slice(-1)[0]?.action!==a)g+=.5;if(repeat>=2)g*=.55;return Math.max(.35,g)}
function applyTraits(a,matched,repeat){const t=S.traits;if(a==='pat'){t.clingy+=matched?1.8:1;t.gentle+=.5;if(!matched||repeat>=2)t.overcare+=.8+repeat*.15}if(a==='play'){t.playful+=matched?1.5:1;t.clingy+=.25;if(repeat>=3)t.overcare+=.4}if(['feed','bath','toilet'].includes(a)){t.gentle+=matched?1:.45;if(matched)t.independent+=.55;if(!matched&&repeat>=2)t.overcare+=.35}if(a==='sleep')t.gentle+=.4}
function stageChanged(old){const n=stage()[0];if(old===n)return;const lines={baby:'……でた。',child:'なんか育った。',adult:'これがおとな。'};S.lastSpeech=lines[n]||S.lastSpeech;setTimeout(()=>setPet(basePet(),0,'bounce'),50)}
