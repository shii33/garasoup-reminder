async function act(a){if(stage()[0]==='egg')return;const sleeping=isAsleep();if(sleeping&&a!=='sleep'){S.lastSpeech=entryFor('sleep').text;recordRecent('speech',S.lastSpeech);await save();render();return}if(a==='sleep'&&sleeping){if(canWakeNow()){S.manualSleepUntil=0;S.morningWakeDate=today();const r=contextualSpeech('wake',true,0);S.lastSpeech=r.entry.text;currentSpeechEntry=r.entry;recordRecent('speech',S.lastSpeech);await save();render();setPet('stand_front.png',900,'bounce')}else{S.lastSpeech=entryFor('sleep').text;recordRecent('speech',S.lastSpeech);await save();render()}return}if(a==='sleep'&&!canSleepNow()){S.lastSpeech=entryFor('idle').text;recordRecent('speech',S.lastSpeech);await save();render();return}const before=stage()[0],m=moment(),matched=m.action===a,rep=repeats(a);S.totalCare=Number(S.totalCare||0)+1;S.actions[a]=Number(S.actions[a]||0)+1;S.lastActionAt[a]=now();S.experience=Number(S.experience||0)+experienceGain(a,matched,rep);applyTraits(a,matched,rep);if(a==='feed'){S.poopDueAt=now()+(90+Math.random()*150)*60000}if(a==='toilet'){S.poop=false;S.poopDueAt=0}if(a==='sleep'){markNight();S.manualSleepUntil=nextNine();S.morningWakeDate=''}const r=contextualSpeech(a,matched,rep);S.lastSpeech=r.entry.text;currentSpeechEntry=r.entry;S.recent.push({at:now(),type:'action',action:a,text:S.lastSpeech});if(S.recent.length>80)S.recent=S.recent.slice(-80);resolveEvolution();stageChanged(before);const mem=maybeMemory();if(mem){S.lastSpeech=mem.entry.text;currentSpeechEntry=mem.entry}await save();render();const rf=reactionPet(a,rep,r.rare);if(rf)setPet(rf,1250,a==='play'?'bounce':a==='pat'?'squish':'wiggle')}
const EGG={warm:['🫶','あたためる'],call:['📣','名前を呼ぶ'],tap:['👉','つんつん'],hum:['🎵','うたう'],listen:['👂','ようすを見る'],word:['🌀','われわれ語']};
async function eggAct(a){const before=stage()[0];S.eggActions[a]=Number(S.eggActions[a]||0)+1;S.totalCare=Number(S.totalCare||0)+1;S.experience+=1;const e=entryFor(a==='hum'?'play':a==='call'?'ack':'short');const short=cleanText(e.text).slice(0,20);S.lastSpeech='……'+short;currentSpeechEntry=e;recordRecent('speech',S.lastSpeech);resolveEvolution();stageChanged(before);await save();render();setPet(basePet(),700,'wiggle')}
function profileLines(){return[]}
function recentCareSummary(){const labels={feed:['🍚','ごはん'],play:['🎮','あそぶ'],pat:['💗','愛でる'],bath:['🫧','おふろ'],toilet:['🚽','かたづける']};const rows=(S.recent||[]).filter(x=>x&&x.type==='action'&&labels[x.action]).slice(-24);const counts={};for(const x of rows)counts[x.action]=Number(counts[x.action]||0)+1;let top=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0];if(!top){top=Object.entries(S.actions||{}).filter(([k,v])=>labels[k]&&Number(v)>0).sort((a,b)=>Number(b[1])-Number(a[1]))[0]}if(!top)return'まだ、これといってない。';const l=labels[top[0]];return`${l[0]} ${l[1]}　いちばん多い`}
function recentPhraseSummary(){const counts={};for(const x of (S.recent||[]).slice(-60)){const t=cleanText(x?.text||'');if(!t||t.length>36||BLOCK.test(t))continue;counts[t]=Number(counts[t]||0)+1}const top=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0];return top?.[0]||cleanText(S.lastSpeech||'')}
function recentRareMoment(){const rows=(S.recent||[]).slice().reverse();for(const x of rows){const t=cleanText(x?.text||'');if(!t||!RX.rareLove.test(t))continue;const d=Number(x.at||0)?new Date(Number(x.at)):null;const date=d&&!Number.isNaN(d.getTime())?`${d.getMonth()+1}/${d.getDate()}`:'';return{text:t,date}}return null}
function topFavorites(){return Object.entries(S.favoritePhrases||{}).sort((a,b)=>b[1]-a[1]).slice(0,5).map(x=>x[0])}
function renderAbout(){const box=$('g12about');if(!box)return;const side=box.closest('.g12side'),heading=side?.querySelector('h2');if(heading)heading.textContent='この子の中身';const care=recentCareSummary(),phrase=recentPhraseSummary(),fav=topFavorites(),rare=recentRareMoment();const rows=[`<p><b>最近よくされてること</b><br>${esc(care)}</p>`,`<p><b>最近よく言う</b><br>${phrase?`「${esc(phrase)}」`:'まだ、口ぐせは分からない。'}</p>`,`<p><b>覚えてる言い方</b><br>${fav.length?`♡ ${fav.length}こ`:'まだない。'}</p>`];if(rare)rows.push(`<p><b>この前のやつ</b><br>「${esc(rare.text)}」${rare.date?` <small>${esc(rare.date)}</small>`:''}</p>`);box.innerHTML=rows.join('');const words=$('g12words');if(words)words.innerHTML=fav.map(x=>`<span class="g12word">${esc(x)}</span>`).join('');const note=$('g12lognote');if(note)note.textContent='お世話したこと、よく言うこと、好き登録した言い方がこの子に残っていく。'}
function actionButtons(){const m=moment();if(stage()[0]==='egg'){return`<div class="g12action-grid">${Object.entries(EGG).slice(0,4).map(([k,v])=>`<button class="g12action" data-e="${k}"><i>${v[0]}</i><b>${v[1]}</b></button>`).join('')}</div><div class="g12context">${Object.entries(EGG).slice(4).map(([k,v])=>`<button data-e="${k}">${v[0]} ${v[1]}</button>`).join('')}</div>`}const asleep=isAsleep(),d={feed:['🍚','ごはん','食べさせる'],play:['🎮','あそぶ','ちょっかい出す'],pat:['💗','愛でる','とりあえず触る'],bath:['🫧','おふろ','カラダ・アラウ']};let h=`<div class="g12action-grid">${Object.entries(d).map(([k,v])=>`<button class="g12action ${!asleep&&m.action===k?'rec':''}" data-a="${k}" ${asleep?'disabled':''}><i>${v[0]}</i><b>${v[1]}</b><small>${asleep?'ねてる':v[2]}</small></button>`).join('')}</div>`;const extra=[];if(S.poop)extra.push(`<button data-a="toilet">🚽 かたづける</button>`);if(canSleepNow()||asleep)extra.push(`<button data-a="sleep">${asleep?(canWakeNow()?'☀️ 起こす':'😴 ねてる'):'🌙 ねる'}</button>`);if(extra.length)h+=`<div class="g12context">${extra.join('')}</div>`;return h}
function render(){if(!S)return;resolveEvolution();const d=new Date(),st=stage(),m=moment();$('g12room').classList.toggle('night',d.getHours()<6||d.getHours()>=18);$('g12name').textContent=N[G];$('g12stage').textContent=`${ageDays()}日目・${st[1]}`;$('g12status').textContent=`${m.emoji} ${m.label}`;$('g12speech').textContent=S.lastSpeech||entryFor('idle').text;$('g12petbox').className=`g12petbox s-${st[0]}`;setPet(currentPet());const bl=branchLabel();$('g12branch').hidden=!bl;$('g12branch').textContent=bl;$('g12poop').hidden=!S.poop;$('g12actions').innerHTML=actionButtons();const mem=currentMemory;$('g12memory').hidden=!mem;if(mem){$('g12memory').innerHTML=`<b>記憶がよぎった</b>${esc(mem.date||'いつかのわれわれ')} · この日の前後を見る`}const liked=!!(currentSpeechEntry&&S.favoritePhrases?.[currentSpeechEntry.text]);$('g12like').disabled=!currentSpeechEntry;$('g12like').classList.toggle('liked',liked);$('g12like').setAttribute('aria-pressed',liked?'true':'false');$('g12like').textContent=liked?'♥ この言い方すき':'♡ この言い方すき';renderAbout();save()}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function openMemory(){if(!currentMemory)return;const lines=(currentMemory.lines||[]).filter(x=>x&&usable(x.text));$('g12dlg').innerHTML=`<h2>${esc(currentMemory.date||'いつか')} のわれわれ</h2>${lines.map(x=>`<div class="g12line"><span class="g12who">${esc(x.who)}</span><div>${esc(cleanText(x.text)).replace(/\n/g,'<br>')}</div></div>`).join('')}<button class="g12close" id="g12close">閉じる</button>`;$('g12modal').hidden=false;$('g12close').onclick=()=>{$('g12modal').hidden=true}}
async function likeSpeech(){if(!currentSpeechEntry)return;const t=cleanText(currentSpeechEntry.text);if(!t)return;if(!S.favoritePhrases[t])S.favoritePhrases[t]=3;await save();render()}
